{
  "name": "handshake-task-alert-extension",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "dev": "node build.mjs --watch",
    "check": "tsc --noEmit -p tsconfig.json"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.277",
    "esbuild": "^0.24.0",
    "typescript": "^5.6.2"
  }
}
