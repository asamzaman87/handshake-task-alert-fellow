# Backend root URL (no trailing slash). Passed into the service worker at build time.
# Default production build uses https://handshake-task-alert-fellow.vercel.app
# override for local backend:
EXTENSION_BACKEND_URL=http://127.0.0.1:8787
